name: diagram
description: "Experimental diagram API in Flutter"
version: 0.1.0
repository: https://github.com/caduandrade/diagram_flutter

topics:
  - ui
  - diagram

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

